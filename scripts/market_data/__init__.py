"""Market-data adapters.

Currently ships:
  str_manual — parser for an analyst-supplied STR-style comp-set CSV
               (hospitality fallback while STR API access is TBD).

CoStar adapters and Census / BLS / HUD / FRED clients are still on the
roadmap (see workflows/02-market-analysis.md).
"""
